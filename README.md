# BIO — Business Intelligence Officer

Portal app (tile **14**, slug `bio-business-intelligence-officer`) for **Tereo, LLC**. FastAPI + ASGI service with Portal EdDSA SSO (`portal_token_bio`), stub BI dashboard (example metrics only), and open `/healthz`.

See DEPLOY.md and run `pytest -q`.
